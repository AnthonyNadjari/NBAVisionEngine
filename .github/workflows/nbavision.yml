# NBAVision Engine — GitHub Actions (Section 2.1)
# Runtime max 6h, Python 3.11, Playwright Chromium, secrets: TWITTER_COOKIES_JSON, LLM_API_KEY

name: NBAVision Engine

on:
  workflow_dispatch:
  schedule:
    # Run once per day (adjust as needed)
    - cron: "0 14 * * *"

jobs:
  run:
    runs-on: ubuntu-latest
    timeout-minutes: 360  # 6 hours max (Section 2.1)

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Install Playwright Chromium
        run: playwright install --with-deps chromium

      - name: Run NBAVision Engine
        env:
          TWITTER_COOKIES_JSON: ${{ secrets.TWITTER_COOKIES_JSON }}
          LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
        run: python main.py

      - name: Upload session logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: session-logs
          path: logs/
          if-no-files-found: ignore
